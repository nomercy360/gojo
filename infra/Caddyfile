{$DOMAIN:localhost} {
	# API — all backend routes under /api/*
	handle /api/* {
		uri strip_prefix /api
		reverse_proxy api:3001
	}

	# S3/Minio public assets
	handle_path /s3/* {
		reverse_proxy minio:9000
	}

	# LiveKit WebSocket
	handle_path /lk-ws/* {
		reverse_proxy livekit:7880
	}

	# Everything else → Next.js
	handle {
		reverse_proxy web:3000
	}
}
